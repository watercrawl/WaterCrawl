VITE_API_URL=http://localhost:8000
VITE_VERSION=development

# Sentry Configuration
# ------------------------------------------------------------------
# Build-time only (for uploading source maps - keep these secret!)
# Required when running 'pnpm build' to upload source maps to Sentry
# Note: Also configure these as GitHub Secrets for CI/CD (see GITHUB_SECRETS.md)
SENTRY_AUTH_TOKEN=
SENTRY_ORG=
SENTRY_PROJECT=

# Runtime (shared with users - can be public)
# Required for error tracking in production
VITE_SENTRY_DSN=